import 'package:flutter/material.dart';

class NearMeScrren extends StatelessWidget {
  const NearMeScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('nearMeScrren'),
    );
  }
}
