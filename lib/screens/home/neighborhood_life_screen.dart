import 'package:flutter/material.dart';

class NeighborhoodLifeScrren extends StatelessWidget {
  const NeighborhoodLifeScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('neighborhoodLifeScreen'),
    );
  }
}
